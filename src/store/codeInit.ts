// Legacy module — model loading is now handled by the worker via slug-based imports.
// Kept as a minimal stub for potential future use.

export default async function codeInit(_modelSlug?: string): Promise<string> {
  return "";
}
