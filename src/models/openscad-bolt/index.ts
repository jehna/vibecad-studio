import type { ModelDefinition } from "../types";

const model: ModelDefinition = {
  name: "Bolt (OpenSCAD)",
  description:
    "A parametric hex bolt generated via the TypeScript → OpenSCAD pipeline",
  slug: "openscad-bolt",
  type: "openscad",
};

export default model;
